
回复按钮点击, 改变 state.

右侧UI组件感知到 state 的变化, 渲染出最新的 UI.
感知的方式, 是通过高阶组件方式, 抽取 state 中的值, 通过props传递子组件.

mattermost 组件通常:
1. 组件一个目录
2. 组件名.tsx 放组件的核心UI逻辑
3. index.tsx 导出, 但会抽取 state 到 props
4. type.ts, 如果类型比较多, 还会抽出单独type.ts文件

如:
export default connect(mapStateToProps, mapDispatchToProps)(业务UI组件);