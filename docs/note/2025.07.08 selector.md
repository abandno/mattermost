

# createSelector 设计

export function createSelectorCreator(memoize, ...memoizeOptions)
webapp/channels/src/packages/mattermost-redux/src/selectors/create_selector/index.js:57 

const resultFunc = funcs.pop();  最后参数是结果收集用的.
```js
(name, ...funcs) => {
    return () -> {
        return funcs[-1]; // 这返回最终结果
    }
}
```

名字, 函数列表.
前面的函数取值, 最后一个汇聚结果. 前面函数取值的结果刚好对应最后一个函数的入参.
N+1 函数链.

返回值是selector function.